language: node_js
node_js:
  - "node"

cache: 
  directories:
  - node_modules

branches:
  only:
    - develop

before_script:
  - npm i -g @angular/cli

script:
  - ng build --prod --buildOptimizer

deploy:
  provider: firebase
  token:
    secure: $FIREBASE_TOKEN
  skip_cleanup: true
  on:
    branch: develop

notifications:
  email:
    on_failure: change
    on_success: change
